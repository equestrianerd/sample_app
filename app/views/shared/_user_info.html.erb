<div class="user_info">
	<a href="<%= user_path(current_user) %>">
		<%= gravatar_for current_user, :size => 10 %>
		<span class="user_name">
			<%= current_user.name %>
		</span>
		<div class="microposts">
			<%= pluralize(current_user.microposts.count, "micropost") %>
		</div>
	</a>
</div>
